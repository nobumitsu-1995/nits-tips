import { style } from '@vanilla-extract/css'
import { vars } from '../theme.css'

export const nicoKaku = style({
  fontFamily: `${vars.font.nicoKaku} !important`,
})

export const bizUD = style({
  fontFamily: `${vars.font.bizUD} !important`,
})
