import { style } from '@vanilla-extract/css'

export const list = style({
  listStyle: 'square',
  paddingLeft: 20,
})

export const numberList = style({
  listStyle: 'decimal',
  paddingLeft: 20,
})
