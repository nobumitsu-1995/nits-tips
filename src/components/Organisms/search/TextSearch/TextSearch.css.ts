import { style } from '@vanilla-extract/css'

export const textSearch = style({})

export const formContainer = style({
  marginTop: 24,
  maxWidth: 570,
  display: 'flex',
  flexDirection: 'column',
  gap: 12,
})
